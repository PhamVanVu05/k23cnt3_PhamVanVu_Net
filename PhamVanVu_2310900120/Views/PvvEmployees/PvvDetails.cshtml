model PhamVanVu_2310900120.Models.PvvEmployee

4">Họ tên</dt>
<dd class="col-sm-8">@Model.PvvEmpName</dd>

<dt class="col-sm-4">Trình độ</dt>
<dd class="col-sm-8">@Model.PvvEmpLevel</dd>

<dt class="col-sm-4">Ngày bắt đầu</dt>
<dd class="col-sm-8">@Model.PvvEmpStartDate?.ToString("dd/MM/yyyy")</dd>

<dt class="col-sm-4">Trạng thái</dt>
<dd class="col-sm-8">
    @if (Model.PvvEmpStatus == true)
    {
        <span class="badge bg-success">Hiển thị</span>
    }
    else
    {
        <span class="badge bg-secondary">Tạm khóa</span>
    }
</dd>
</dl>
</div>

<div class="card-footer text-center">
    <a asp-action="PvvEdit" asp-route-PvvId="@Model.PvvEmpId" class="btn btn-warning me-2">
        ✏️ Chỉnh sửa
    </a>
    <a asp-action="PvvIndex" class="btn btn-secondary">
        ⬅️ Quay lại danh sách
    </a>
</div>
</div>
